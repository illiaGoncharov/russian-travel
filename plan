+ initial + nojekyll added + basic index markup + basic file str. + images + normalize.css + basic readme update
+ readme upd + inter.css & font files added + vendor import normalize & font
+ markup upd + block file structure + import block's css-s
+ header & mq-s + body.css + page.css
+ lead & mq-s + optimized lead__image + optimized smoothing & rendering + logo size
+ optimized logo + intro & mq-s + fonts update
+ photo-grid & mq-s
+ optimized imgs + places & place + mq-s
+ cover & mq-s + footer $ mq-s
+ "ru"=lang + lead img to .jpg [opt to 30kb] + font files updated
+ cover__link min-height-s upd + footer opacity .8 + lead upd + place opacity .8

---

=

- Мобильный дизайн у вас стартует для всех секций с разрешения 767 - при таком отображении получаются очень большие картинки в places, а в cover узкая длинная картинка. Я бы рекомендовала немного поменять брейкпойнты для этих секций или добавить какие-то промежуточные.

- Для задания внешней геометрии и позиционирования блока можно использовать микс с элементом родительского блока https://ru.bem.info/methodology/css/#%D0%B2%D0%BD%D0%B5%D1%88%D0%BD%D1%8F%D1%8F-%D0%B3%D0%B5%D0%BE%D0%BC%D0%B5%D1%82%D1%80%D0%B8%D1%8F-%D0%B8-%D0%BF%D0%BE%D0%B7%D0%B8%D1%86%D0%B8%D0%BE%D0%BD%D0%B8%D1%
