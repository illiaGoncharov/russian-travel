+ initial + nojekyll added + basic index markup + basic file str. + images + normalize.css + basic readme update 
+ readme upd + inter.css & font files added + vendor import normalize & font

---

= markup uod + block file structure + import block's css-s

- header brunch 
- 
